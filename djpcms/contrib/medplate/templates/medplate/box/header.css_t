{% extends "medplate/elem.css_t" %}

{% block body %}
{{ tag }} h2 {
    float: left;
    margin:0;
    padding:0;
    font-weight: {{ font_weight }};
    font-size: {{ title_size }};{% if text_transform %}
    text-transform: {{ text_transform }};{% endif %}
}
{% endblock %}