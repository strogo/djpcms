{% extends "medplate/elem.css_t" %}

{% block body %}

{{ tag }} a {
	font-family:{{ font_family }};
	font-style:{{ font_style }};
	font-weight: {{ font_weight }};
}
{{ tag }} ul,
{{ tag }} ul li {
	list-style: none;
	display:inline;
}
{% endblock %}